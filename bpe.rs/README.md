A Rust implementation of BPE tokenizer.

## Instruction

First download the enwik8 corpus:

```bash
wget https://mattmahoney.net/dc/enwik8.zip
unzip enwik8.zip
```

and then run:

```bash
cargo run --release -- --corpus enwik8
```
